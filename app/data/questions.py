"""SQL practice questions for MySQL and PostgreSQL.

Structure:
  QUESTIONS[db_type][difficulty] = list of question dicts

Each question dict has:
  id        - int, unique within db_type+difficulty
  topic     - str, topic label shown to user
  question  - str, the question text
  hint      - str, optional hint shown on demand
  sample_answer - str, a correct sample answer (shown after attempt)
"""

# ---------------------------------------------------------------------------
# MySQL Questions
# ---------------------------------------------------------------------------

MYSQL_BEGINNER = [
    {
        "id": 1,
        "topic": "SELECT Basics",
        "question": "Write a query to select all columns and all rows from the employees table.",
        "hint": "Use SELECT * FROM table_name;",
        "sample_answer": "SELECT * FROM employees;",
    },
    {
        "id": 2,
        "topic": "SELECT Columns",
        "question": "Write a query to select only the name and department columns from the employees table.",
        "hint": "List the column names separated by commas after SELECT.",
        "sample_answer": "SELECT name, department FROM employees;",
    },
    {
        "id": 3,
        "topic": "WHERE Clause",
        "question": "Write a query to fetch all employees who work in the 'Engineering' department.",
        "hint": "Use WHERE department = 'Engineering'",
        "sample_answer": "SELECT * FROM employees WHERE department = 'Engineering';",
    },
    {
        "id": 4,
        "topic": "WHERE with Numbers",
        "question": "Write a query to fetch all employees with a salary greater than 90000.",
        "hint": "Use WHERE salary > 90000",
        "sample_answer": "SELECT * FROM employees WHERE salary > 90000;",
    },
    {
        "id": 5,
        "topic": "ORDER BY",
        "question": "Write a query to select all employees ordered by their salary in descending order.",
        "hint": "Use ORDER BY salary DESC",
        "sample_answer": "SELECT * FROM employees ORDER BY salary DESC;",
    },
    {
        "id": 6,
        "topic": "ORDER BY ASC",
        "question": "Write a query to select all employees ordered by name in ascending (alphabetical) order.",
        "hint": "Use ORDER BY name ASC or just ORDER BY name",
        "sample_answer": "SELECT * FROM employees ORDER BY name ASC;",
    },
    {
        "id": 7,
        "topic": "LIMIT",
        "question": "Write a query to fetch the top 3 employees ordered by salary descending.",
        "hint": "Use ORDER BY salary DESC LIMIT 3",
        "sample_answer": "SELECT * FROM employees ORDER BY salary DESC LIMIT 3;",
    },
    {
        "id": 8,
        "topic": "DISTINCT",
        "question": "Write a query to get a list of all unique departments from the employees table.",
        "hint": "Use SELECT DISTINCT column_name",
        "sample_answer": "SELECT DISTINCT department FROM employees;",
    },
    {
        "id": 9,
        "topic": "COUNT",
        "question": "Write a query to count the total number of employees.",
        "hint": "Use COUNT(*) or COUNT(id)",
        "sample_answer": "SELECT COUNT(*) FROM employees;",
    },
    {
        "id": 10,
        "topic": "Aggregate Functions",
        "question": "Write a query to find the maximum salary in the employees table.",
        "hint": "Use SELECT MAX(salary)",
        "sample_answer": "SELECT MAX(salary) FROM employees;",
    },
    {
        "id": 11,
        "topic": "Aggregate Functions",
        "question": "Write a query to find the minimum salary in the employees table.",
        "hint": "Use SELECT MIN(salary)",
        "sample_answer": "SELECT MIN(salary) FROM employees;",
    },
    {
        "id": 12,
        "topic": "Aggregate Functions",
        "question": "Write a query to find the average salary of all employees.",
        "hint": "Use SELECT AVG(salary)",
        "sample_answer": "SELECT AVG(salary) FROM employees;",
    },
    {
        "id": 13,
        "topic": "Aggregate Functions",
        "question": "Write a query to calculate the total (SUM) of all employee salaries.",
        "hint": "Use SELECT SUM(salary)",
        "sample_answer": "SELECT SUM(salary) FROM employees;",
    },
    {
        "id": 14,
        "topic": "WHERE with AND",
        "question": "Write a query to fetch employees in the 'Engineering' department with salary greater than 95000.",
        "hint": "Use WHERE department = '...' AND salary > ...",
        "sample_answer": "SELECT * FROM employees WHERE department = 'Engineering' AND salary > 95000;",
    },
    {
        "id": 15,
        "topic": "WHERE with OR",
        "question": "Write a query to fetch employees from either 'HR' or 'Finance' departments.",
        "hint": "Use WHERE department = 'HR' OR department = 'Finance'",
        "sample_answer": "SELECT * FROM employees WHERE department = 'HR' OR department = 'Finance';",
    },
    {
        "id": 16,
        "topic": "CREATE TABLE",
        "question": "Create a new table called 'departments' with columns: id (INT, PRIMARY KEY, AUTO_INCREMENT) and dept_name (VARCHAR(100)).",
        "hint": "Use CREATE TABLE departments (id INT PRIMARY KEY AUTO_INCREMENT, ...)",
        "sample_answer": "CREATE TABLE departments (id INT PRIMARY KEY AUTO_INCREMENT, dept_name VARCHAR(100));",
    },
    {
        "id": 17,
        "topic": "INSERT",
        "question": "Insert a new employee into the employees table: name='Test User', department='Finance', salary=75000, hire_date='2024-01-01'.",
        "hint": "Use INSERT INTO employees (name, department, salary, hire_date) VALUES (...)",
        "sample_answer": "INSERT INTO employees (name, department, salary, hire_date) VALUES ('Test User', 'Finance', 75000, '2024-01-01');",
    },
    {
        "id": 18,
        "topic": "UPDATE",
        "question": "Update the salary of the employee named 'Bob Smith' to 80000.",
        "hint": "Use UPDATE employees SET salary = 80000 WHERE name = 'Bob Smith'",
        "sample_answer": "UPDATE employees SET salary = 80000 WHERE name = 'Bob Smith';",
    },
    {
        "id": 19,
        "topic": "DELETE",
        "question": "Delete all employees from the HR department.",
        "hint": "Use DELETE FROM employees WHERE department = 'HR'",
        "sample_answer": "DELETE FROM employees WHERE department = 'HR';",
    },
    {
        "id": 20,
        "topic": "SHOW TABLES",
        "question": "Write a query to list all tables in your database.",
        "hint": "Use SHOW TABLES;",
        "sample_answer": "SHOW TABLES;",
    },
    {
        "id": 21,
        "topic": "DESCRIBE",
        "question": "Write a query to show the structure (columns and data types) of the employees table.",
        "hint": "Use DESCRIBE or DESC table_name",
        "sample_answer": "DESCRIBE employees;",
    },
    {
        "id": 22,
        "topic": "ALTER TABLE",
        "question": "Add a new column 'email' of type VARCHAR(200) to the employees table.",
        "hint": "Use ALTER TABLE employees ADD COLUMN email VARCHAR(200)",
        "sample_answer": "ALTER TABLE employees ADD COLUMN email VARCHAR(200);",
    },
    {
        "id": 23,
        "topic": "SELECT with ALIAS",
        "question": "Write a query to select employee name as 'Employee Name' and salary as 'Monthly Salary' from employees.",
        "hint": "Use column AS 'alias name'",
        "sample_answer": "SELECT name AS 'Employee Name', salary AS 'Monthly Salary' FROM employees;",
    },
    {
        "id": 24,
        "topic": "NULL Checks",
        "question": "Write a query to find all employees where the department is NULL.",
        "hint": "Use WHERE department IS NULL",
        "sample_answer": "SELECT * FROM employees WHERE department IS NULL;",
    },
    {
        "id": 25,
        "topic": "NOT NULL",
        "question": "Write a query to find all employees where department is NOT NULL.",
        "hint": "Use WHERE department IS NOT NULL",
        "sample_answer": "SELECT * FROM employees WHERE department IS NOT NULL;",
    },
    {
        "id": 26,
        "topic": "SELECT with Calculation",
        "question": "Write a query to show each employee's name and their annual salary (multiply monthly salary by 12).",
        "hint": "Use salary * 12 in your SELECT",
        "sample_answer": "SELECT name, salary * 12 AS annual_salary FROM employees;",
    },
    {
        "id": 27,
        "topic": "LIKE Pattern",
        "question": "Write a query to find all employees whose name starts with 'A'.",
        "hint": "Use WHERE name LIKE 'A%'",
        "sample_answer": "SELECT * FROM employees WHERE name LIKE 'A%';",
    },
    {
        "id": 28,
        "topic": "Products Table",
        "question": "Write a query to select all products with a price less than 100.",
        "hint": "Use SELECT * FROM products WHERE price < 100",
        "sample_answer": "SELECT * FROM products WHERE price < 100;",
    },
    {
        "id": 29,
        "topic": "GROUP BY",
        "question": "Write a query to count the number of employees in each department.",
        "hint": "Use GROUP BY department with COUNT(*)",
        "sample_answer": "SELECT department, COUNT(*) AS employee_count FROM employees GROUP BY department;",
    },
    {
        "id": 30,
        "topic": "ORDER BY Multiple",
        "question": "Write a query to select all employees ordered first by department (ascending) then by salary (descending).",
        "hint": "Use ORDER BY department ASC, salary DESC",
        "sample_answer": "SELECT * FROM employees ORDER BY department ASC, salary DESC;",
    },
]

MYSQL_MODERATE = [
    {
        "id": 31,
        "topic": "INNER JOIN",
        "question": "Write a query to join the orders table with the employees table to show each order's employee name and total price.",
        "hint": "Use INNER JOIN ... ON orders.employee_id = employees.id",
        "sample_answer": "SELECT e.name, o.total_price FROM orders o INNER JOIN employees e ON o.employee_id = e.id;",
    },
    {
        "id": 32,
        "topic": "JOIN with Products",
        "question": "Write a query to show each order with the product name and quantity ordered.",
        "hint": "JOIN orders with products on product_id",
        "sample_answer": "SELECT p.name AS product_name, o.quantity FROM orders o INNER JOIN products p ON o.product_id = p.id;",
    },
    {
        "id": 33,
        "topic": "LEFT JOIN",
        "question": "Write a query using LEFT JOIN to show all employees and their orders (including employees with no orders).",
        "hint": "Use LEFT JOIN orders ON employees.id = orders.employee_id",
        "sample_answer": "SELECT e.name, o.total_price FROM employees e LEFT JOIN orders o ON e.id = o.employee_id;",
    },
    {
        "id": 34,
        "topic": "GROUP BY with HAVING",
        "question": "Write a query to find departments where the average salary is greater than 80000.",
        "hint": "Use GROUP BY department HAVING AVG(salary) > 80000",
        "sample_answer": "SELECT department, AVG(salary) AS avg_salary FROM employees GROUP BY department HAVING AVG(salary) > 80000;",
    },
    {
        "id": 35,
        "topic": "Subquery",
        "question": "Write a query to find all employees who earn more than the average salary.",
        "hint": "Use WHERE salary > (SELECT AVG(salary) FROM employees)",
        "sample_answer": "SELECT * FROM employees WHERE salary > (SELECT AVG(salary) FROM employees);",
    },
    {
        "id": 36,
        "topic": "IN Clause",
        "question": "Write a query to find employees in the 'Engineering', 'Finance', or 'Marketing' departments using IN.",
        "hint": "Use WHERE department IN ('Engineering', 'Finance', 'Marketing')",
        "sample_answer": "SELECT * FROM employees WHERE department IN ('Engineering', 'Finance', 'Marketing');",
    },
    {
        "id": 37,
        "topic": "NOT IN",
        "question": "Write a query to find all products not in the 'Electronics' category.",
        "hint": "Use WHERE category NOT IN ('Electronics')",
        "sample_answer": "SELECT * FROM products WHERE category NOT IN ('Electronics');",
    },
    {
        "id": 38,
        "topic": "BETWEEN",
        "question": "Write a query to find employees with a salary between 70000 and 100000.",
        "hint": "Use WHERE salary BETWEEN 70000 AND 100000",
        "sample_answer": "SELECT * FROM employees WHERE salary BETWEEN 70000 AND 100000;",
    },
    {
        "id": 39,
        "topic": "LIKE Pattern",
        "question": "Write a query to find all products whose name contains the word 'Desk'.",
        "hint": "Use WHERE name LIKE '%Desk%'",
        "sample_answer": "SELECT * FROM products WHERE name LIKE '%Desk%';",
    },
    {
        "id": 40,
        "topic": "Three-Table JOIN",
        "question": "Write a query joining employees, orders, and products to show the employee name, product name, and total price for each order.",
        "hint": "JOIN orders with employees and products",
        "sample_answer": "SELECT e.name AS employee, p.name AS product, o.total_price FROM orders o JOIN employees e ON o.employee_id = e.id JOIN products p ON o.product_id = p.id;",
    },
    {
        "id": 41,
        "topic": "Aggregate with JOIN",
        "question": "Write a query to find the total amount spent by each employee (sum of their orders).",
        "hint": "JOIN orders with employees, GROUP BY employee name, SUM total_price",
        "sample_answer": "SELECT e.name, SUM(o.total_price) AS total_spent FROM employees e JOIN orders o ON e.id = o.employee_id GROUP BY e.name;",
    },
    {
        "id": 42,
        "topic": "Subquery with IN",
        "question": "Write a query to find all employees who have placed at least one order, using a subquery.",
        "hint": "Use WHERE id IN (SELECT employee_id FROM orders)",
        "sample_answer": "SELECT * FROM employees WHERE id IN (SELECT DISTINCT employee_id FROM orders);",
    },
    {
        "id": 43,
        "topic": "DATE Functions",
        "question": "Write a query to find all employees hired in the year 2020.",
        "hint": "Use YEAR(hire_date) = 2020",
        "sample_answer": "SELECT * FROM employees WHERE YEAR(hire_date) = 2020;",
    },
    {
        "id": 44,
        "topic": "String Functions",
        "question": "Write a query to display each employee's name in uppercase.",
        "hint": "Use UPPER(name)",
        "sample_answer": "SELECT UPPER(name) AS name_upper FROM employees;",
    },
    {
        "id": 45,
        "topic": "CONCAT",
        "question": "Write a query to combine the employee name and department into a single column like 'Alice Johnson - Engineering'.",
        "hint": "Use CONCAT(name, ' - ', department)",
        "sample_answer": "SELECT CONCAT(name, ' - ', department) AS employee_info FROM employees;",
    },
    {
        "id": 46,
        "topic": "CASE Expression",
        "question": "Write a query to categorize employees as 'High' (salary >= 90000), 'Mid' (salary >= 70000), or 'Low' otherwise.",
        "hint": "Use CASE WHEN salary >= 90000 THEN 'High' WHEN salary >= 70000 THEN 'Mid' ELSE 'Low' END",
        "sample_answer": "SELECT name, salary, CASE WHEN salary >= 90000 THEN 'High' WHEN salary >= 70000 THEN 'Mid' ELSE 'Low' END AS salary_band FROM employees;",
    },
    {
        "id": 47,
        "topic": "EXISTS",
        "question": "Write a query to find all products that appear in at least one order, using EXISTS.",
        "hint": "Use WHERE EXISTS (SELECT 1 FROM orders WHERE orders.product_id = products.id)",
        "sample_answer": "SELECT * FROM products WHERE EXISTS (SELECT 1 FROM orders WHERE orders.product_id = products.id);",
    },
    {
        "id": 48,
        "topic": "ROUND",
        "question": "Write a query to show the average salary of each department, rounded to 2 decimal places.",
        "hint": "Use ROUND(AVG(salary), 2)",
        "sample_answer": "SELECT department, ROUND(AVG(salary), 2) AS avg_salary FROM employees GROUP BY department;",
    },
    {
        "id": 49,
        "topic": "ORDER BY with LIMIT",
        "question": "Write a query to find the top 5 most expensive products.",
        "hint": "Use ORDER BY price DESC LIMIT 5",
        "sample_answer": "SELECT * FROM products ORDER BY price DESC LIMIT 5;",
    },
    {
        "id": 50,
        "topic": "Multi-column GROUP BY",
        "question": "Write a query to count orders grouped by both employee_id and the year of order_date.",
        "hint": "Use GROUP BY employee_id, YEAR(order_date)",
        "sample_answer": "SELECT employee_id, YEAR(order_date) AS order_year, COUNT(*) AS order_count FROM orders GROUP BY employee_id, YEAR(order_date);",
    },
]

MYSQL_MASTER = [
    {
        "id": 51,
        "topic": "Window Functions - ROW_NUMBER",
        "question": "Write a query using ROW_NUMBER() to rank employees within each department by salary (highest first).",
        "hint": "Use ROW_NUMBER() OVER (PARTITION BY department ORDER BY salary DESC)",
        "sample_answer": "SELECT name, department, salary, ROW_NUMBER() OVER (PARTITION BY department ORDER BY salary DESC) AS row_num FROM employees;",
    },
    {
        "id": 52,
        "topic": "Window Functions - RANK",
        "question": "Write a query using RANK() to rank all employees by salary (highest first). Ties should get the same rank.",
        "hint": "Use RANK() OVER (ORDER BY salary DESC)",
        "sample_answer": "SELECT name, salary, RANK() OVER (ORDER BY salary DESC) AS salary_rank FROM employees;",
    },
    {
        "id": 53,
        "topic": "Window Functions - DENSE_RANK",
        "question": "Write a query using DENSE_RANK() to rank employees by salary. Unlike RANK, DENSE_RANK has no gaps.",
        "hint": "Use DENSE_RANK() OVER (ORDER BY salary DESC)",
        "sample_answer": "SELECT name, salary, DENSE_RANK() OVER (ORDER BY salary DESC) AS dense_rank FROM employees;",
    },
    {
        "id": 54,
        "topic": "Window Functions - LAG",
        "question": "Write a query using LAG() to show each employee's salary and the salary of the previous employee (ordered by hire_date).",
        "hint": "Use LAG(salary) OVER (ORDER BY hire_date)",
        "sample_answer": "SELECT name, hire_date, salary, LAG(salary) OVER (ORDER BY hire_date) AS prev_salary FROM employees;",
    },
    {
        "id": 55,
        "topic": "Window Functions - LEAD",
        "question": "Write a query using LEAD() to show each employee's salary and the salary of the next employee ordered by hire_date.",
        "hint": "Use LEAD(salary) OVER (ORDER BY hire_date)",
        "sample_answer": "SELECT name, hire_date, salary, LEAD(salary) OVER (ORDER BY hire_date) AS next_salary FROM employees;",
    },
    {
        "id": 56,
        "topic": "CTE (WITH clause)",
        "question": "Write a query using a CTE to find the top earner in each department.",
        "hint": "Use WITH cte AS (SELECT ..., ROW_NUMBER() OVER ...) SELECT * FROM cte WHERE row_num = 1",
        "sample_answer": "WITH ranked AS (SELECT name, department, salary, ROW_NUMBER() OVER (PARTITION BY department ORDER BY salary DESC) AS rn FROM employees) SELECT name, department, salary FROM ranked WHERE rn = 1;",
    },
    {
        "id": 57,
        "topic": "CREATE VIEW",
        "question": "Create a view called 'high_earners' that shows all employees with salary > 90000.",
        "hint": "Use CREATE VIEW view_name AS SELECT ...",
        "sample_answer": "CREATE OR REPLACE VIEW high_earners AS SELECT * FROM employees WHERE salary > 90000;",
    },
    {
        "id": 58,
        "topic": "CREATE INDEX",
        "question": "Create an index named 'idx_dept' on the department column of the employees table.",
        "hint": "Use CREATE INDEX idx_dept ON employees(department)",
        "sample_answer": "CREATE INDEX idx_dept ON employees(department);",
    },
    {
        "id": 59,
        "topic": "SHOW INDEXES",
        "question": "Write a query to show all indexes on the employees table.",
        "hint": "Use SHOW INDEXES FROM employees",
        "sample_answer": "SHOW INDEXES FROM employees;",
    },
    {
        "id": 60,
        "topic": "Recursive CTE",
        "question": "Write a query using a recursive CTE to generate a series of numbers from 1 to 5.",
        "hint": "Use WITH RECURSIVE cte AS (SELECT 1 AS n UNION ALL SELECT n+1 FROM cte WHERE n < 5)",
        "sample_answer": "WITH RECURSIVE nums AS (SELECT 1 AS n UNION ALL SELECT n + 1 FROM nums WHERE n < 5) SELECT n FROM nums;",
    },
    {
        "id": 61,
        "topic": "Stored Procedure",
        "question": "Create a stored procedure called 'get_dept_employees' that takes a department name as input and returns all employees in that department.",
        "hint": "Use CREATE PROCEDURE name(IN param TYPE) BEGIN ... END",
        "sample_answer": "CREATE PROCEDURE get_dept_employees(IN dept_name VARCHAR(100)) BEGIN SELECT * FROM employees WHERE department = dept_name; END;",
    },
    {
        "id": 62,
        "topic": "NTILE Window Function",
        "question": "Write a query using NTILE(4) to divide employees into 4 salary quartiles.",
        "hint": "Use NTILE(4) OVER (ORDER BY salary)",
        "sample_answer": "SELECT name, salary, NTILE(4) OVER (ORDER BY salary) AS quartile FROM employees;",
    },
    {
        "id": 63,
        "topic": "Running Total",
        "question": "Write a query to calculate a running total of salaries ordered by hire_date using SUM as a window function.",
        "hint": "Use SUM(salary) OVER (ORDER BY hire_date ROWS UNBOUNDED PRECEDING)",
        "sample_answer": "SELECT name, hire_date, salary, SUM(salary) OVER (ORDER BY hire_date ROWS UNBOUNDED PRECEDING) AS running_total FROM employees;",
    },
    {
        "id": 64,
        "topic": "PIVOT (using CASE)",
        "question": "Write a query to pivot the employee count per department using CASE expressions, showing Engineering, Marketing, and Finance counts in one row.",
        "hint": "Use SUM(CASE WHEN department = '...' THEN 1 ELSE 0 END) for each department",
        "sample_answer": "SELECT SUM(CASE WHEN department = 'Engineering' THEN 1 ELSE 0 END) AS Engineering, SUM(CASE WHEN department = 'Marketing' THEN 1 ELSE 0 END) AS Marketing, SUM(CASE WHEN department = 'Finance' THEN 1 ELSE 0 END) AS Finance FROM employees;",
    },
    {
        "id": 65,
        "topic": "Full Text Search",
        "question": "Write a query to find all products where the name contains 'Pro' using a LIKE pattern (simulating a search).",
        "hint": "Use WHERE name LIKE '%Pro%'",
        "sample_answer": "SELECT * FROM products WHERE name LIKE '%Pro%';",
    },
]

# ---------------------------------------------------------------------------
# PostgreSQL Questions
# ---------------------------------------------------------------------------

POSTGRES_BEGINNER = [
    {
        "id": 1,
        "topic": "SELECT Basics",
        "question": "Write a query to select all columns and all rows from the employees table.",
        "hint": "Use SELECT * FROM table_name;",
        "sample_answer": "SELECT * FROM employees;",
    },
    {
        "id": 2,
        "topic": "SELECT Columns",
        "question": "Write a query to select only the name and department columns from the employees table.",
        "hint": "List the column names separated by commas after SELECT.",
        "sample_answer": "SELECT name, department FROM employees;",
    },
    {
        "id": 3,
        "topic": "WHERE Clause",
        "question": "Write a query to fetch all employees who work in the 'Engineering' department.",
        "hint": "Use WHERE department = 'Engineering'",
        "sample_answer": "SELECT * FROM employees WHERE department = 'Engineering';",
    },
    {
        "id": 4,
        "topic": "WHERE with Numbers",
        "question": "Write a query to fetch all employees with a salary greater than 90000.",
        "hint": "Use WHERE salary > 90000",
        "sample_answer": "SELECT * FROM employees WHERE salary > 90000;",
    },
    {
        "id": 5,
        "topic": "ORDER BY",
        "question": "Write a query to select all employees ordered by their salary in descending order.",
        "hint": "Use ORDER BY salary DESC",
        "sample_answer": "SELECT * FROM employees ORDER BY salary DESC;",
    },
    {
        "id": 6,
        "topic": "ORDER BY ASC",
        "question": "Write a query to select all employees ordered by name in ascending (alphabetical) order.",
        "hint": "Use ORDER BY name ASC",
        "sample_answer": "SELECT * FROM employees ORDER BY name ASC;",
    },
    {
        "id": 7,
        "topic": "LIMIT",
        "question": "Write a query to fetch the top 3 employees ordered by salary descending.",
        "hint": "Use ORDER BY salary DESC LIMIT 3",
        "sample_answer": "SELECT * FROM employees ORDER BY salary DESC LIMIT 3;",
    },
    {
        "id": 8,
        "topic": "DISTINCT",
        "question": "Write a query to get a list of all unique departments from the employees table.",
        "hint": "Use SELECT DISTINCT column_name",
        "sample_answer": "SELECT DISTINCT department FROM employees;",
    },
    {
        "id": 9,
        "topic": "COUNT",
        "question": "Write a query to count the total number of employees.",
        "hint": "Use COUNT(*) or COUNT(id)",
        "sample_answer": "SELECT COUNT(*) FROM employees;",
    },
    {
        "id": 10,
        "topic": "Aggregate Functions",
        "question": "Write a query to find the maximum salary in the employees table.",
        "hint": "Use SELECT MAX(salary)",
        "sample_answer": "SELECT MAX(salary) FROM employees;",
    },
    {
        "id": 11,
        "topic": "Aggregate Functions",
        "question": "Write a query to find the minimum salary in the employees table.",
        "hint": "Use SELECT MIN(salary)",
        "sample_answer": "SELECT MIN(salary) FROM employees;",
    },
    {
        "id": 12,
        "topic": "Aggregate Functions",
        "question": "Write a query to find the average salary of all employees.",
        "hint": "Use SELECT AVG(salary)",
        "sample_answer": "SELECT AVG(salary) FROM employees;",
    },
    {
        "id": 13,
        "topic": "Aggregate Functions",
        "question": "Write a query to calculate the total (SUM) of all employee salaries.",
        "hint": "Use SELECT SUM(salary)",
        "sample_answer": "SELECT SUM(salary) FROM employees;",
    },
    {
        "id": 14,
        "topic": "WHERE with AND",
        "question": "Write a query to fetch employees in the 'Engineering' department with salary greater than 95000.",
        "hint": "Use WHERE department = '...' AND salary > ...",
        "sample_answer": "SELECT * FROM employees WHERE department = 'Engineering' AND salary > 95000;",
    },
    {
        "id": 15,
        "topic": "WHERE with OR",
        "question": "Write a query to fetch employees from either 'HR' or 'Finance' departments.",
        "hint": "Use WHERE department = 'HR' OR department = 'Finance'",
        "sample_answer": "SELECT * FROM employees WHERE department = 'HR' OR department = 'Finance';",
    },
    {
        "id": 16,
        "topic": "CREATE TABLE",
        "question": "Create a new table called 'departments' with columns: id (SERIAL PRIMARY KEY) and dept_name (VARCHAR(100)).",
        "hint": "In PostgreSQL, use SERIAL instead of AUTO_INCREMENT",
        "sample_answer": "CREATE TABLE departments (id SERIAL PRIMARY KEY, dept_name VARCHAR(100));",
    },
    {
        "id": 17,
        "topic": "INSERT with RETURNING",
        "question": "Insert a new employee: name='Test User', department='Finance', salary=75000, hire_date='2024-01-01'. Return the id of the inserted row.",
        "hint": "In PostgreSQL, add RETURNING id at the end of INSERT",
        "sample_answer": "INSERT INTO employees (name, department, salary, hire_date) VALUES ('Test User', 'Finance', 75000, '2024-01-01') RETURNING id;",
    },
    {
        "id": 18,
        "topic": "UPDATE",
        "question": "Update the salary of the employee named 'Bob Smith' to 80000.",
        "hint": "Use UPDATE employees SET salary = 80000 WHERE name = 'Bob Smith'",
        "sample_answer": "UPDATE employees SET salary = 80000 WHERE name = 'Bob Smith';",
    },
    {
        "id": 19,
        "topic": "DELETE",
        "question": "Delete all employees from the HR department.",
        "hint": "Use DELETE FROM employees WHERE department = 'HR'",
        "sample_answer": "DELETE FROM employees WHERE department = 'HR';",
    },
    {
        "id": 20,
        "topic": "List Tables",
        "question": "Write a query to list all tables in your current schema using information_schema.",
        "hint": "In PostgreSQL, use SELECT table_name FROM information_schema.tables WHERE table_schema = 'public'",
        "sample_answer": "SELECT table_name FROM information_schema.tables WHERE table_schema = 'public';",
    },
    {
        "id": 21,
        "topic": "Describe Table",
        "question": "Write a query to show the column names and data types of the employees table using information_schema.",
        "hint": "Use SELECT column_name, data_type FROM information_schema.columns WHERE table_name = 'employees'",
        "sample_answer": "SELECT column_name, data_type FROM information_schema.columns WHERE table_name = 'employees';",
    },
    {
        "id": 22,
        "topic": "ALTER TABLE",
        "question": "Add a new column 'email' of type VARCHAR(200) to the employees table.",
        "hint": "Use ALTER TABLE employees ADD COLUMN email VARCHAR(200)",
        "sample_answer": "ALTER TABLE employees ADD COLUMN email VARCHAR(200);",
    },
    {
        "id": 23,
        "topic": "SELECT with ALIAS",
        "question": "Write a query to select employee name as 'Employee Name' and salary as 'Monthly Salary' from employees.",
        "hint": "Use column AS \"alias name\" (double quotes in PostgreSQL)",
        "sample_answer": "SELECT name AS \"Employee Name\", salary AS \"Monthly Salary\" FROM employees;",
    },
    {
        "id": 24,
        "topic": "NULL Checks",
        "question": "Write a query to find all employees where the department is NULL.",
        "hint": "Use WHERE department IS NULL",
        "sample_answer": "SELECT * FROM employees WHERE department IS NULL;",
    },
    {
        "id": 25,
        "topic": "NOT NULL",
        "question": "Write a query to find all employees where department is NOT NULL.",
        "hint": "Use WHERE department IS NOT NULL",
        "sample_answer": "SELECT * FROM employees WHERE department IS NOT NULL;",
    },
    {
        "id": 26,
        "topic": "SELECT with Calculation",
        "question": "Write a query to show each employee's name and their annual salary (multiply monthly salary by 12).",
        "hint": "Use salary * 12 in your SELECT",
        "sample_answer": "SELECT name, salary * 12 AS annual_salary FROM employees;",
    },
    {
        "id": 27,
        "topic": "ILIKE Pattern",
        "question": "Write a query to find all employees whose name starts with 'a' (case-insensitive) using ILIKE.",
        "hint": "Use WHERE name ILIKE 'a%' — PostgreSQL's case-insensitive LIKE",
        "sample_answer": "SELECT * FROM employees WHERE name ILIKE 'a%';",
    },
    {
        "id": 28,
        "topic": "Products Table",
        "question": "Write a query to select all products with a price less than 100.",
        "hint": "Use SELECT * FROM products WHERE price < 100",
        "sample_answer": "SELECT * FROM products WHERE price < 100;",
    },
    {
        "id": 29,
        "topic": "GROUP BY",
        "question": "Write a query to count the number of employees in each department.",
        "hint": "Use GROUP BY department with COUNT(*)",
        "sample_answer": "SELECT department, COUNT(*) AS employee_count FROM employees GROUP BY department;",
    },
    {
        "id": 30,
        "topic": "ORDER BY Multiple",
        "question": "Write a query to select all employees ordered first by department (ascending) then by salary (descending).",
        "hint": "Use ORDER BY department ASC, salary DESC",
        "sample_answer": "SELECT * FROM employees ORDER BY department ASC, salary DESC;",
    },
]

POSTGRES_MODERATE = [
    {
        "id": 31,
        "topic": "INNER JOIN",
        "question": "Write a query to join the orders table with the employees table to show each order's employee name and total price.",
        "hint": "Use INNER JOIN ... ON orders.employee_id = employees.id",
        "sample_answer": "SELECT e.name, o.total_price FROM orders o INNER JOIN employees e ON o.employee_id = e.id;",
    },
    {
        "id": 32,
        "topic": "JOIN with Products",
        "question": "Write a query to show each order with the product name and quantity ordered.",
        "hint": "JOIN orders with products on product_id",
        "sample_answer": "SELECT p.name AS product_name, o.quantity FROM orders o INNER JOIN products p ON o.product_id = p.id;",
    },
    {
        "id": 33,
        "topic": "LEFT JOIN",
        "question": "Write a query using LEFT JOIN to show all employees and their orders (including employees with no orders).",
        "hint": "Use LEFT JOIN orders ON employees.id = orders.employee_id",
        "sample_answer": "SELECT e.name, o.total_price FROM employees e LEFT JOIN orders o ON e.id = o.employee_id;",
    },
    {
        "id": 34,
        "topic": "GROUP BY with HAVING",
        "question": "Write a query to find departments where the average salary is greater than 80000.",
        "hint": "Use GROUP BY department HAVING AVG(salary) > 80000",
        "sample_answer": "SELECT department, AVG(salary) AS avg_salary FROM employees GROUP BY department HAVING AVG(salary) > 80000;",
    },
    {
        "id": 35,
        "topic": "Subquery",
        "question": "Write a query to find all employees who earn more than the average salary.",
        "hint": "Use WHERE salary > (SELECT AVG(salary) FROM employees)",
        "sample_answer": "SELECT * FROM employees WHERE salary > (SELECT AVG(salary) FROM employees);",
    },
    {
        "id": 36,
        "topic": "IN Clause",
        "question": "Write a query to find employees in the 'Engineering', 'Finance', or 'Marketing' departments using IN.",
        "hint": "Use WHERE department IN ('Engineering', 'Finance', 'Marketing')",
        "sample_answer": "SELECT * FROM employees WHERE department IN ('Engineering', 'Finance', 'Marketing');",
    },
    {
        "id": 37,
        "topic": "NOT IN",
        "question": "Write a query to find all products not in the 'Electronics' category.",
        "hint": "Use WHERE category NOT IN ('Electronics')",
        "sample_answer": "SELECT * FROM products WHERE category NOT IN ('Electronics');",
    },
    {
        "id": 38,
        "topic": "BETWEEN",
        "question": "Write a query to find employees with a salary between 70000 and 100000.",
        "hint": "Use WHERE salary BETWEEN 70000 AND 100000",
        "sample_answer": "SELECT * FROM employees WHERE salary BETWEEN 70000 AND 100000;",
    },
    {
        "id": 39,
        "topic": "ILIKE Pattern",
        "question": "Write a query to find all products whose name contains 'desk' (case-insensitive).",
        "hint": "Use WHERE name ILIKE '%desk%'",
        "sample_answer": "SELECT * FROM products WHERE name ILIKE '%desk%';",
    },
    {
        "id": 40,
        "topic": "Three-Table JOIN",
        "question": "Write a query joining employees, orders, and products to show the employee name, product name, and total price for each order.",
        "hint": "JOIN orders with employees and products",
        "sample_answer": "SELECT e.name AS employee, p.name AS product, o.total_price FROM orders o JOIN employees e ON o.employee_id = e.id JOIN products p ON o.product_id = p.id;",
    },
    {
        "id": 41,
        "topic": "Aggregate with JOIN",
        "question": "Write a query to find the total amount spent by each employee (sum of their orders).",
        "hint": "JOIN orders with employees, GROUP BY employee name, SUM total_price",
        "sample_answer": "SELECT e.name, SUM(o.total_price) AS total_spent FROM employees e JOIN orders o ON e.id = o.employee_id GROUP BY e.name;",
    },
    {
        "id": 42,
        "topic": "Subquery with IN",
        "question": "Write a query to find all employees who have placed at least one order, using a subquery.",
        "hint": "Use WHERE id IN (SELECT employee_id FROM orders)",
        "sample_answer": "SELECT * FROM employees WHERE id IN (SELECT DISTINCT employee_id FROM orders);",
    },
    {
        "id": 43,
        "topic": "DATE Functions",
        "question": "Write a query to find all employees hired in the year 2020 using EXTRACT.",
        "hint": "Use EXTRACT(YEAR FROM hire_date) = 2020",
        "sample_answer": "SELECT * FROM employees WHERE EXTRACT(YEAR FROM hire_date) = 2020;",
    },
    {
        "id": 44,
        "topic": "String Functions",
        "question": "Write a query to display each employee's name in uppercase using PostgreSQL's UPPER function.",
        "hint": "Use UPPER(name)",
        "sample_answer": "SELECT UPPER(name) AS name_upper FROM employees;",
    },
    {
        "id": 45,
        "topic": "CONCAT / ||",
        "question": "Write a query to combine the employee name and department using the PostgreSQL string concatenation operator ||.",
        "hint": "Use name || ' - ' || department",
        "sample_answer": "SELECT name || ' - ' || department AS employee_info FROM employees;",
    },
    {
        "id": 46,
        "topic": "CASE Expression",
        "question": "Write a query to categorize employees as 'High' (salary >= 90000), 'Mid' (salary >= 70000), or 'Low' otherwise.",
        "hint": "Use CASE WHEN salary >= 90000 THEN 'High' ... END",
        "sample_answer": "SELECT name, salary, CASE WHEN salary >= 90000 THEN 'High' WHEN salary >= 70000 THEN 'Mid' ELSE 'Low' END AS salary_band FROM employees;",
    },
    {
        "id": 47,
        "topic": "EXISTS",
        "question": "Write a query to find all products that appear in at least one order, using EXISTS.",
        "hint": "Use WHERE EXISTS (SELECT 1 FROM orders WHERE orders.product_id = products.id)",
        "sample_answer": "SELECT * FROM products WHERE EXISTS (SELECT 1 FROM orders WHERE orders.product_id = products.id);",
    },
    {
        "id": 48,
        "topic": "ROUND",
        "question": "Write a query to show the average salary of each department, rounded to 2 decimal places.",
        "hint": "Use ROUND(AVG(salary)::numeric, 2) in PostgreSQL",
        "sample_answer": "SELECT department, ROUND(AVG(salary)::numeric, 2) AS avg_salary FROM employees GROUP BY department;",
    },
    {
        "id": 49,
        "topic": "ORDER BY with LIMIT",
        "question": "Write a query to find the top 5 most expensive products.",
        "hint": "Use ORDER BY price DESC LIMIT 5",
        "sample_answer": "SELECT * FROM products ORDER BY price DESC LIMIT 5;",
    },
    {
        "id": 50,
        "topic": "Multi-column GROUP BY",
        "question": "Write a query to count orders grouped by employee_id and the year extracted from order_date.",
        "hint": "Use GROUP BY employee_id, EXTRACT(YEAR FROM order_date)",
        "sample_answer": "SELECT employee_id, EXTRACT(YEAR FROM order_date) AS order_year, COUNT(*) AS order_count FROM orders GROUP BY employee_id, EXTRACT(YEAR FROM order_date);",
    },
]

POSTGRES_MASTER = [
    {
        "id": 51,
        "topic": "Window Functions - ROW_NUMBER",
        "question": "Write a query using ROW_NUMBER() to rank employees within each department by salary (highest first).",
        "hint": "Use ROW_NUMBER() OVER (PARTITION BY department ORDER BY salary DESC)",
        "sample_answer": "SELECT name, department, salary, ROW_NUMBER() OVER (PARTITION BY department ORDER BY salary DESC) AS row_num FROM employees;",
    },
    {
        "id": 52,
        "topic": "Window Functions - RANK",
        "question": "Write a query using RANK() to rank all employees by salary (highest first).",
        "hint": "Use RANK() OVER (ORDER BY salary DESC)",
        "sample_answer": "SELECT name, salary, RANK() OVER (ORDER BY salary DESC) AS salary_rank FROM employees;",
    },
    {
        "id": 53,
        "topic": "Window Functions - DENSE_RANK",
        "question": "Write a query using DENSE_RANK() to rank employees by salary without gaps in ranking.",
        "hint": "Use DENSE_RANK() OVER (ORDER BY salary DESC)",
        "sample_answer": "SELECT name, salary, DENSE_RANK() OVER (ORDER BY salary DESC) AS dense_rank FROM employees;",
    },
    {
        "id": 54,
        "topic": "Window Functions - LAG",
        "question": "Write a query using LAG() to show each employee's salary and the previous employee's salary ordered by hire_date.",
        "hint": "Use LAG(salary) OVER (ORDER BY hire_date)",
        "sample_answer": "SELECT name, hire_date, salary, LAG(salary) OVER (ORDER BY hire_date) AS prev_salary FROM employees;",
    },
    {
        "id": 55,
        "topic": "Window Functions - LEAD",
        "question": "Write a query using LEAD() to show each employee's salary and the next employee's salary ordered by hire_date.",
        "hint": "Use LEAD(salary) OVER (ORDER BY hire_date)",
        "sample_answer": "SELECT name, hire_date, salary, LEAD(salary) OVER (ORDER BY hire_date) AS next_salary FROM employees;",
    },
    {
        "id": 56,
        "topic": "CTE (WITH clause)",
        "question": "Write a query using a CTE to find the top earner in each department.",
        "hint": "Use WITH ranked AS (SELECT ..., ROW_NUMBER() OVER ...) SELECT * FROM ranked WHERE rn = 1",
        "sample_answer": "WITH ranked AS (SELECT name, department, salary, ROW_NUMBER() OVER (PARTITION BY department ORDER BY salary DESC) AS rn FROM employees) SELECT name, department, salary FROM ranked WHERE rn = 1;",
    },
    {
        "id": 57,
        "topic": "CREATE VIEW",
        "question": "Create a view called 'high_earners' that shows all employees with salary > 90000.",
        "hint": "Use CREATE OR REPLACE VIEW view_name AS SELECT ...",
        "sample_answer": "CREATE OR REPLACE VIEW high_earners AS SELECT * FROM employees WHERE salary > 90000;",
    },
    {
        "id": 58,
        "topic": "CREATE INDEX",
        "question": "Create an index named 'idx_dept' on the department column of the employees table.",
        "hint": "Use CREATE INDEX idx_dept ON employees(department)",
        "sample_answer": "CREATE INDEX IF NOT EXISTS idx_dept ON employees(department);",
    },
    {
        "id": 59,
        "topic": "List Indexes (pg_indexes)",
        "question": "Write a query to show all indexes on the employees table using pg_indexes.",
        "hint": "Use SELECT * FROM pg_indexes WHERE tablename = 'employees'",
        "sample_answer": "SELECT indexname, indexdef FROM pg_indexes WHERE tablename = 'employees';",
    },
    {
        "id": 60,
        "topic": "Recursive CTE",
        "question": "Write a query using a recursive CTE to generate a series of numbers from 1 to 5.",
        "hint": "Use WITH RECURSIVE cte AS (SELECT 1 AS n UNION ALL SELECT n+1 FROM cte WHERE n < 5)",
        "sample_answer": "WITH RECURSIVE nums AS (SELECT 1 AS n UNION ALL SELECT n + 1 FROM nums WHERE n < 5) SELECT n FROM nums;",
    },
    {
        "id": 61,
        "topic": "UPDATE with RETURNING",
        "question": "Update the salary of employee named 'Frank Wilson' to 82000 and return the updated row.",
        "hint": "Use UPDATE ... SET ... WHERE ... RETURNING *",
        "sample_answer": "UPDATE employees SET salary = 82000 WHERE name = 'Frank Wilson' RETURNING *;",
    },
    {
        "id": 62,
        "topic": "NTILE Window Function",
        "question": "Write a query using NTILE(4) to divide employees into 4 salary quartiles.",
        "hint": "Use NTILE(4) OVER (ORDER BY salary)",
        "sample_answer": "SELECT name, salary, NTILE(4) OVER (ORDER BY salary) AS quartile FROM employees;",
    },
    {
        "id": 63,
        "topic": "Running Total",
        "question": "Write a query to calculate a running total of salaries ordered by hire_date.",
        "hint": "Use SUM(salary) OVER (ORDER BY hire_date ROWS UNBOUNDED PRECEDING)",
        "sample_answer": "SELECT name, hire_date, salary, SUM(salary) OVER (ORDER BY hire_date ROWS UNBOUNDED PRECEDING) AS running_total FROM employees;",
    },
    {
        "id": 64,
        "topic": "GENERATE_SERIES",
        "question": "Write a query using generate_series() to produce a list of dates for the first 7 days of January 2024.",
        "hint": "Use generate_series('2024-01-01'::date, '2024-01-07'::date, '1 day'::interval)",
        "sample_answer": "SELECT generate_series('2024-01-01'::date, '2024-01-07'::date, '1 day'::interval) AS day;",
    },
    {
        "id": 65,
        "topic": "JSON Functions",
        "question": "Write a query to select all employee names and convert the result to a JSON array using json_agg.",
        "hint": "Use SELECT json_agg(name) FROM employees",
        "sample_answer": "SELECT json_agg(name) AS employee_names FROM employees;",
    },
]

# ---------------------------------------------------------------------------
# Public registry
# ---------------------------------------------------------------------------

QUESTIONS = {
    "mysql": {
        "beginner": MYSQL_BEGINNER,
        "moderate": MYSQL_MODERATE,
        "master": MYSQL_MASTER,
    },
    "postgres": {
        "beginner": POSTGRES_BEGINNER,
        "moderate": POSTGRES_MODERATE,
        "master": POSTGRES_MASTER,
    },
}

DIFFICULTY_LABELS = {
    "beginner": {"label": "Beginner", "emoji": "🟢", "color": "#4ade80"},
    "moderate": {"label": "Moderate", "emoji": "🟡", "color": "#facc15"},
    "master":   {"label": "Master",   "emoji": "🔴", "color": "#f87171"},
}
